global proc string[] selected_transforms() {
    string $nodes[] = `ls -sl -type "transform"`;
    string $result[];

    for ($node in $nodes) {
        if (`objExists $node`) {
            $result[size($result)] = $node;
        }
    }

    return $result;
}
